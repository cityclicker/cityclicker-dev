export enum Currency {
    CITYBUX = 'CityBux',
}

export enum ActionResult {
    UnknownError = 'UNKNOWNERROR',
    Success = 'SUCCESS',
    InsuficientFunds = 'INSUFICIENT_FUNDS',
    InvalidInput = 'INVALID_INPUT',
}
